import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';

import { Modal } from '.';
import { Button } from '../Button';

<Meta title="Design System|Modal" component={Modal} />

# Modal

## Graphical control element subordinate to an application's main window.

Note: Can't provide stories for `Modal` component until StoryBook supports state management in mdx.

<Preview>
  <Story name="default">
    <Modal
      isOpen
      header="Header"
      footer={
        <div className="d-flex">
          <Button theme="secondary" className="mr-2">
            Some Action 1
          </Button>
          <Button>Some Action 2</Button>
        </div>
      }
    >
      some content
    </Modal>
  </Story>
</Preview>

<Props of={Modal} />
