import {
  Meta,
  Story,
  Props,
  Preview,
  withToolbar,
} from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';

import FieldOverlap from '.';
import Select from '../Select';
import Input from '../Input';

<Meta title="Design System|Field Overlap" component={FieldOverlap} />

# Field Overlap

<Preview withToolbar>
  <Story name="default">
    <div>
      <FieldOverlap label="Label without content" />
      <FieldOverlap label="Label">Some Content</FieldOverlap>
    </div>
  </Story>
</Preview>

<Props of={FieldOverlap} />

## Usage

### Error

<Preview withToolbar>
  <Story name="error">
    <FieldOverlap label="Label" error>
      Some Content
    </FieldOverlap>
  </Story>
</Preview>

### Loading

<Preview withToolbar>
  <Story name="loading">
    <FieldOverlap label="Label" loading>
      Some Content
    </FieldOverlap>
  </Story>
</Preview>

### Disabled

<Preview withToolbar>
  <Story name="disabled">
    <FieldOverlap label="Label" disabled>
      Some Content
    </FieldOverlap>
  </Story>
</Preview>

### With Input

<Preview withToolbar>
  <Story name="with input">
    <Input />
  </Story>
</Preview>

### With select

<Preview withToolbar>
  <Story name="with select">
    <div>
      <Select
        label="Select values"
        items={[
          { value: 'foo' },
          { value: 'bar' },
          { value: 'baz' },
          { value: 'que' },
        ]}
      />
    </div>
  </Story>
</Preview>

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="knobs" decorators={[withKnobs]}>
  <FieldOverlap
    label="Label"
    loading={boolean('loading')}
    error={boolean('error')}
    resetIcon={boolean('resetIcon')}
  >
    Some content
  </FieldOverlap>
</Story>
