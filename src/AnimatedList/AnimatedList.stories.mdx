import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';

import { AnimatedList } from '.';
import { Avatar } from '../Avatar';
import { Sublabel } from '../Sublabel';

<Meta title="Design System|AnimatedList" component={AnimatedList} />

# AnimatedList

## List of items animated in a sequence

<Preview withToolbar>
  <Story name="default">
    <AnimatedList
      className="d-flex"
      itemClassName="mr-2"
      name="avatars"
      items={[
        { value: 'foo' },
        { value: 'bar' },
        { value: 'quartz' },
        { value: 'ruby' },
        { value: 'topaz' },
      ]}
    >
      {({ meta }) => <Avatar username={meta.value} />}
    </AnimatedList>
  </Story>
</Preview>

<Props of={AnimatedList} />

## Usage

Common use is to enhance static lists with animations to make the content feel alive and dynamic.

### AnimatedList with `Sublabel` as item component

<Preview withToolbar>
  <Story name="with sublabel as component">
    <AnimatedList
      className="d-flex"
      itemClassName="mr-2"
      name="sublabels"
      items={[
        { value: 'foo' },
        { value: 'bar' },
        { value: 'quartz' },
        { value: 'ruby' },
        { value: 'topaz' },
      ]}
    >
      {({ meta }) => <Sublabel>{meta.value}</Sublabel>}
    </AnimatedList>
  </Story>
</Preview>
