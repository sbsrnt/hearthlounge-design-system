import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';

import { Slider } from '.';

<Meta title="Design System|Slider" component={Slider} />

# Slider

## Input control with range of values between a specified min and max.

Proxy of [rc-slider](http://react-component.github.io/slider/).

<Preview withToolbar>
  <Story name="default">
    <Slider onChange={(name, value) => console.log(name, value)} />
  </Story>
</Preview>

<Props of={Slider} />
